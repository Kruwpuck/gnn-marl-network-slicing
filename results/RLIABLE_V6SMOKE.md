# rliable Report — IQM + Stratified Bootstrap 95% CI

Readout: `primary per family — sampled for PPO (P3), argmax for DQN (2026-08-16)`.

Tag filter: `_v6smoke,_v4`. DQN (200K steps) and PPO (1M steps) families are never pooled. A CI overlap means 'comparable', not 'proposed wins' — report accordingly.


## gnn-madqn_gat vs idqn, central-dqn (DQN family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gat` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-madqn_gat` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gat` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-madqn_gat` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-madqn_gat` and at least one baseline)

## gnn-madqn_sage vs idqn, central-dqn (DQN family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_sage` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-madqn_sage` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_sage` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-madqn_sage` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-madqn_sage` and at least one baseline)

## gnn-mappo_gat vs ippo, central-ppo (PPO family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gat` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-mappo_gat` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gat` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-mappo_gat` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-mappo_gat` and at least one baseline)

## gnn-mappo_sage vs ippo, central-ppo (PPO family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_sage` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-mappo_sage` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_sage` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-mappo_sage` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-mappo_sage` and at least one baseline)

## gnn-madqn_gatres vs idqn, central-dqn, gnn-madqn_gat (DQN family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres` and at least one baseline)

## gnn-mappo_gatres vs ippo, central-ppo, gnn-mappo_gat (PPO family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres` and at least one baseline)

## gnn-madqn_gatedge vs idqn, central-dqn, gnn-madqn_gat (DQN family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatedge` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatedge` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatedge` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatedge` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatedge` and at least one baseline)

## gnn-mappo_gatedge vs ippo, central-ppo, gnn-mappo_gat (PPO family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatedge` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatedge` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatedge` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatedge` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatedge` and at least one baseline)

## gnn-madqn_gatres-edge vs idqn, central-dqn, gnn-madqn_gat (DQN family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres-edge` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres-edge` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres-edge` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres-edge` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-madqn_gatres-edge` and at least one baseline)

## gnn-mappo_gatres-edge vs ippo, central-ppo, gnn-mappo_gat (PPO family)

- **timely_throughput_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres-edge` and at least one baseline)
- **sla_satisfaction_pct**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres-edge` and at least one baseline)
- **embb_p5_mbps**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres-edge` and at least one baseline)
- **jains_fairness**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres-edge` and at least one baseline)
- **urllc_delay_p99**: skipped (need >=2 seeds of eval data for `gnn-mappo_gatres-edge` and at least one baseline)